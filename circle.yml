machine:
  services:
    - docker
  ruby:
    version: rbx-2.2.6
  node:
    version: 0.10.22

dependencies:
  override:
    - npm install

test:
  override:
    - npm test
    - docker build -t gcr.io/sentia_analytics/web .
