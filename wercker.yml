build:
  box: google/cloud-sdk
  steps:
    - script:
      name: gcloud
      code: gcloud --help

    - script:
      name: docker build
      code:  gcloud preview docker build gcr.io/sentia-analytics/web .
