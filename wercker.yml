build:
  box: dockercore/docker
  steps:
    - script:
      name: docker build
      code: docker build -t gcr.io/sentia-analytics/web .

    - script:
      name: docker run
      code: sh runDocker.sh
